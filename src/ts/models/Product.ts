export interface Product {
  id: string;
  name: string;
  category: string;
  variant: string;
  description: string;
  price: number;
  weight: number;
  currency: string;
  isNew: boolean;
  image: string;
}
